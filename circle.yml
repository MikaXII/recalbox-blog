machine:
  environment:
    USER_NAME: MikaXII
    USER_EMAIL: mikael.joubert@outlook.com
  ruby:
    version: 2.3.2

dependencies:
  pre:
    - ./bin/setup

test:
  post:
    - bundle exec jekyll build
    - bundle exec htmlproofer ./_site --only-4xx --allow-hash-href true

deployment:
  production:
    branch: master
    commands:
      - ./bin/automated
